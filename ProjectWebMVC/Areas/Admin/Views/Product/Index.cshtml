@model List<Product>

<div class="card shadow border-0 my-4">
    <div class="card-header bg-info bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white">Book List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3 col-12">
            <div class="text-end">
                <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i>   Create New Book
                </a>
            </div>
        </div>

        <table id="tblData" class="table table-bordered table-striped table-hover" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Book Title
                    </th>
                    <th>
                        ISBN
                    </th>
                    <th>
                        Author
                    </th>
                    <th>
                        Price
                    </th>
                    <th>
                        Category
                    </th>
                    <th>

                    </th>
                </tr>
            </thead>
            @* <tbody>

                @foreach (var obj in Model)
                {
                    <tr>
                        <td>
                            @obj.Title
                        </td>
                        <td>
                            @obj.ISBN
                        </td>
                        <td>
                            @obj.Author
                        </td>
                        <td>
                            @obj.ListPrice
                        </td>
                        <td>
                            @obj.Category.Name
                        </td>
                        <td>
                            <div class="p-1 w-75 btn-group" role="group">
                                <a asp-controller="Product" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-primary mx-2 rounded">
                                    <i class="bi bi-pencil-square"></i> Update
                                </a>

                                <a asp-controller="Product" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-secondary mx-2 rounded">
                                    <i class="bi bi-x-circle"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody> *@
        </table>
    </div>
</div>


@section Scripts {
    <script src="~/js/product.js"></script>
}